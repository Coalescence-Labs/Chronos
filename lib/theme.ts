/**
 * Browser-chrome colors for surfaces that cannot read CSS variables
 * (PWA manifest, meta theme-color). Mirrors --bg in app/globals.css —
 * keep the two in sync when the palette changes.
 */
export const CHROME_BG = "#0a0c10";
